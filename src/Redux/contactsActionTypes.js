const ADD = "phonebook/add_contact";
const REMOVE = "phonebook/remove_contact";

const CHANGE_FILTER = "phonebook/find_contact";

// eslint-disable-next-line import/no-anonymous-default-export
export default { ADD, REMOVE, CHANGE_FILTER };
